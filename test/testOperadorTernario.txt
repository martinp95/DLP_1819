f(a:int) {
	println a;
}

main() {

  var a: int;
  var x: float;
  
  x = 0.0;
  a = 0;
  read a;
  
  println a > 0 ? 1 : 2;
  f(a > 0 ? a + 1 : a - 1);

  //print x ? 0 : 1; // Error x no es entero
  //print a > 0 ? x : 1; // Error x y 1 no son del mismo tipo (o hacer conversion implicita).

}